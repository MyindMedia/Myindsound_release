# Grilled.md — Myind Sound Releases

Alignment record from the 2026-09-16 grilling session. Treat this as authoritative context before planning or building. Append new decisions at the bottom; don't rewrite history.

## Goal

1. Replace the stream page player with a Three.js MiniDisc deck: cyberpunk scene, floating HUD, a cartridge-insert animation, and physical keys that latch.
2. Move the whole backend off Supabase (unreachable since at least 2026-09-16) onto Convex, with paid audio in Cloudflare R2 behind short-lived signed links.

## Scope

Three sub-projects, built in this order:

| # | Sub-project | Spec |
|---|---|---|
| 1 | Convex backend, slice 1: products, tracks, users, entitlements, orders, plays, Stripe webhook, checkout, leads, R2 audio + downloads, buyer rebuild from Stripe | `docs/superpowers/specs/2026-09-16-convex-backend-slice1-design.md` |
| 2 | 3D MiniDisc player on `stream.html` | `docs/superpowers/specs/2026-09-16-minidisc-player-3d-design.md` |
| 3 | Convex backend, slice 2: dashboard, admin, nav admin flag, physical store wiring, retire `stream/` React app, delete remaining Netlify functions and `@supabase/supabase-js` | not written yet |

## Target users

- **LIT buyers:** fans who paid PWYW, often on a phone, often straight from Stripe checkout.
- **Lawrence (admin):** checks plays and orders, and manages products.

## Stack

- **Site:** multi-page Vite + vanilla TypeScript (no React on the main site), hosted on Netlify.
- **Backend:** Convex (database, queries/mutations/actions, HTTP actions, crons), US region.
- **Auth:** Clerk. Convex verifies Clerk JWTs; nothing trusts a client-supplied user ID.
- **Payments:** Stripe Checkout (hosted redirect, SAQ A).
- **Audio files:** Cloudflare R2 through the `@convex-dev/r2` component, with signed URLs (2 h stream, 1 h download).
- **CRM:** Go High Level.
- **Analytics:** PostHog (`src/analytics.ts`, new, uncommitted).
- **3D:** `three` from npm, GSAP (already loaded from CDN) for timelines, HTML HUD layer.
- **Design assets:** Canva design `DAHAUDNdp9k` ("CD PLYAER MYINDSOUND", 18 pages), exported as transparent PNGs through the Canva MCP.

## Constraints

- **Netlify only, never Vercel.** Netlify serves the static site; its build runs `npx convex deploy --cmd 'npm run build'`.
- **Compliance (Compliance Ops check, 2026-09-16):**
  - Claude never reads customer PII. Migration and debug output print counts and IDs only; real records are browsed in the Convex dashboard by Lawrence.
  - Export + delete-my-data must cascade to Clerk and GHL (Stripe keeps payment records for tax).
  - Plays are pruned after 12 months.
  - Marketing consent is an explicit, unticked opt-in with a stored timestamp.
  - No emails in logs.
  - R2 holds audio only, never PII, and no user IDs in object keys.
  - Card data never touches our code (Stripe Checkout redirect).
- **Brand:** `#1A1A1A` base, gold `#FDB913`, orange `#FF8C00`, Inter. The player adds LIT pink `#FF3DA8` and ice `#9FD8FF` for the scene only. Monospace (JetBrains Mono) for HUD numerals only.
- **Accessibility:** real `<button>`s behind the 3D keys, keyboard operable, and a reduced-motion variant (no fly-in, rain or sway).
- **Performance:** 60 fps target on a 2021 iPhone, DPR capped (2 desktop / 1.5 phone), textures WebP ≤ 2048 px, no rendering while the tab is hidden, static fallback when WebGL is unavailable.
- **Security gate:** `/pentest` before calling either backend slice done.
- **Known Convex/Clerk traps on this machine** (from memory):
  - A Clerk JWT template named `convex` must exist on each Clerk instance (dev and prod).
  - A `prebuild` runs `convex codegen` because `--cmd` builds before codegen.
  - Run Convex CLI under Homebrew `node@22` (Node 25 breaks codegen).
  - User-facing errors use `ConvexError`, because production redacts plain `Error`.

## Non-goals

- A full 360° model of the player; it's front-on with visible thickness only.
- A seek bar or a dedicated now-playing panel (time lives in the readouts, the current track glows in the tracklist).
- Gyroscope tilt on phones.
- Rebuilding the home-page post-purchase reveal in Three.js (it keeps handing off to `stream.html?state=animate_dock`).
- Key-click sound effects.
- Restoring Supabase play history or admin flags (lost with the project).
- A cookie-consent banner (see open questions).

## Decisions log (2026-09-16)

| Question | Decision |
|---|---|
| Transparent assets | Connect Canva MCP, export all 18 pages as transparent PNGs → `~/Downloads/CD PLYAER MYINDSOUND/transparent/` |
| Player scope | Stream page only; insert animation replaces the dock animation |
| 3D depth | Solid front-on: art on front faces, dark `#1A1A1A` edges, tilt ≤ ~30° |
| Insert trigger | Tap to insert (one gesture unlocks audio) |
| Keys | Pause, Stop, ◀ prev track, ▶ next track, Play, Red = repeat. Play/Pause/Red latch down; Stop pops all |
| HUD | Live spectrum, tracklist panel, volume + readouts; time folded into readouts |
| Background | Built in the same Three.js scene; Unicorn Studio removed from the stream page |
| Palette | Brand gold/orange HUD, LIT pink/ice scene |
| Build approach | Vanilla Three.js + HTML HUD, ThreeUI effects ported without React (`crt`, `condensation`, `uplink-loader`, `dot-matrix`) |
| Backend | Supabase → Convex, all of it, two slices |
| Paid audio storage | Cloudflare R2 via `@convex-dev/r2`, signed links |
| Existing buyers | Rebuild from Stripe paid checkout sessions |
| Audio masters | `~/Downloads/LIT  [Live In Truth]-2026-09-17/` (5 MP3 ~190 kbps VBR, 1 WAV 16-bit/48 kHz) |
| Compliance fixes | Accepted (see Constraints) |

## Asset map (Canva page → role)

| Page | Role |
|---|---|
| 13 | Player body (window cut through) |
| 7 | Inner back wall behind the window |
| 10 | Glare layer over the window |
| 8 | "WAY UP" slot door |
| 9 | Key strip → cut into 6 keys |
| 16 | Cartridge shell (disc area masked out) |
| 5 | LIT disc (spins) |
| 3 | Clear disc layer (spins) |
| 2 | Myind Sound label (fixed) |
| 11 | Composite reference for placement only |
| 1, 4, 6, 12, 14, 15, 17, 18 | Unused for now |

## Open questions

1. **Album title:** "Live In Truth" (masters folder) or "Living In Truth" (track 1 filename and current code)? Asked twice, unanswered.
2. **Production domain(s)** for the R2 CORS allow-list and Clerk. Not in `netlify.toml`, and memory says the apex is a different Netlify site.
3. **Cloudflare account:** does Lawrence have one with R2 enabled?
4. **Clerk:** do the dev and prod instances already have a `convex` JWT template?
5. **PostHog:** cookieless mode, or add a consent banner for EU visitors?
6. **`create-physical-checkout`** has no frontend caller in `src/` or the HTML. Is the physical store on Shopify (`src/shopify.ts`) instead? Resolve in slice 2.
7. **THE SOURCE presale:** Stripe product `prod_TsqUkQtzNQ5Y3z` links to an untitled.stream placeholder URL. Is the real link known?
8. **Security follow-up:** once Convex replaces them, delete `get-stream-urls` and `get-download-url` promptly. Both trust a client-sent user ID today.

## Build session updates (2026-09-16, later)

| Topic | Decision / finding |
|---|---|
| Coloured analyser, matching eject | Lawrence: "make the spectergram small er and each specturm has a color and put it on the right under the deck MD01 HUD" and "the eject animation needs to be the same as load logo where it zooms out slightly during transtion". The analyser moved from the full width of the bottom into the right column under DECK MD-01 (grid row 88 px to 66 px, the canvas 54 px tall), and every band now carries its own colour, mixed along the scene's own ramp: pink, orange, gold, ice, bass to treble, each bar glowing in its own colour. The eject borrows the load's camera move: it eases back to dolly 1.3 / look 0.45 as the catch releases and the cartridge springs out, then settles to 1 / 0 as it floats, so the two transitions read as the same gesture (measured: 1 → 1.3 → 1 across the 5 s) |
| Bigger deck, the song on the phone's bar | Lawrence: "The player needs to show up larger on the computer browser scrren" and "the lower section on mobile ... The track list area should show the current song playeing on mobile view". On a desktop the deck now fills 1.22 of the HUD's frame rather than 0.94 (the frame is a layout hint, and there is nothing above or below it to collide with), the empty band above it is 104 px instead of 150, and a new clamp stops it growing past 86% of the window height on a tall screen. Phones and tablets keep 0.94 and their own rows, so nothing there changes size. The phone's collapsed tracklist bar reads the song that is playing (`01 · L.I.T. (LIVING IN TRUTH)`, ellipsised) instead of `TRACKLIST 01/07 · PREVIEWS`; it falls back to the count when no disc is loaded |
| The music follows you round the site | Lawrence: "if some one navigates form the player to the site. keep the music playing and make a small song conntorls on the botom right", "if it is the same session and they navigate back to the min page by pass the unwrap sequence ... only after a site refresh then they see the reavael sequence", "after a paruchse thre should be a way to go right to the player with out unwrapping again", and "the album image should be the mini disk with the spinning disk inside". The site is separate pages, so the player's audio element dies on every link. `playback-handoff.ts` writes the tracklist, the track, the place and whether it was running to sessionStorage as the player plays; `mini-player.ts` (on every page but the player) picks it up, adds the page load back on (rolling into the next track if it ran out on the way), and carries on in a small panel in the bottom right, a bar across the bottom on a phone: the cartridge with the LIT disc turning inside it, the title, prev / play / next, a seekable progress line and a close button that stops it. Autoplay refused on the new page leaves it cued with the play button asking for a tap. Media Session metadata is set, so lock screens and headphone buttons work. Coming back to the player is the same handoff in reverse: the packaging is skipped, the disc goes straight back into the deck without the insert timeline (`runInsertSequence(..., instant)`) and carries on where the mini player left it. A **refresh** of the player is still a fresh start, sealed package and all, which is what `isReload()` is for; a purchase lands unwrapped too, and the success page's button is now PLAY THE ALBUM. Sizing the player in `dvh` (from the mobile pass) made the deck smaller than it should be, so that is reverted: the fixed box keeps the whole window |
| One theme on every page | Lawrence: "keep the same theme of huds and color scheem in the enteir site on every page", "I need the store to have things in grids flotin" and "fix the zoom and reactiveness of the mobiele experience". New `theme.css`, linked after `style.css` on all seven pages: it repoints the old palette (#FFD700 gold, Montserrat) at the HUD's own (#FDB913 gold, ink #07070C, cream #F5F1E6, Inter + JetBrains Mono), and gives the nav, the cards, the buttons and the fields the HUD's chrome, gold hairlines, corner ticks and float shadows. Its selectors are `body`-scoped because every page carries its own `<style>` block after the link. The store is a floating grid now (auto-fill from 258 px, 28 px gutters, cards that lift on hover): the hero, the loader and the grid used to be laid out side by side in a row, because `style.css` centres `.main-content` with `display: flex`, so the title sat halfway down the page beside the products. Mobile: gutters come back (style.css took them away under 768 px), product names wrap instead of being clipped mid-word, grids reflow at 150 px and 132 px, every tap target is at least 44 px, sheets clear the home indicator, and the player is sized in `dvh` rather than a fixed `inset: 0`, which on iOS is the large viewport and put the bottom of the HUD under the address bar |
| Quieter packaging, share card | Lawrence: "you can also lower the volume of the unwrap and sleeve pull animation. its a bit too loud" and "make a very nice social share image for this site". The unwrap mix is 0.45 against the music instead of 0.85, and the two one-shots are at 0.9 and 0.85, about 6 dB down in all. The share card is `public/og.jpg` (1200x630), rendered from `scripts/og-card.html`: the city the player is set in, the LIT sleeve under film with a MINIDISC tag, the HUD's gold frame and corner ticks, LIT / LIVING IN TRUTH / THA MYIND · 7 TRACKS · PAY WHAT YOU WANT and the URL. Open the card in the dev server, screenshot it and write it over `public/og.jpg` to re-render. Every page carries the og: and twitter: tags |
| One album level | Lawrence: "check the lufs of all the audio files and make sure they are all the same volume I think he the truth is lower than the other mixes". He was right. Measured with ffmpeg loudnorm (EBU R128): five masters sit between -13.9 and -13.3 LUFS, He The Truth came in at -15.2 and Faith at -11.5, and Faith was clipping at +0.3 dBTP. The album level is now -13.7, recorded in `scripts/lit-tracks.json` with a per-track `gainDb` on the two outliers only (+1.53 and -2.18, a flat gain, no compression, both re-encoded at 320 kbps): everything else is used untouched. The upload script applies the gain to the streamed song and to the copy in the album zip, so a buyer hears one album. The 30-second previews are each levelled to -14 LUFS on their own (they are heard one after another with nothing to compare against), capped to keep -1 dBTP: they were spread over 4.6 LU, they now sit inside 0.2. Coming-soon demos left alone (-15.1 and -14.2), say the word and they follow |
| Nav gets out of the way | Lawrence: "make the nav bar area hidden with a small arrow that will animate and pop out when the mouse hovers in that area. it will auto disappear after 5 seconds of no interaction", then "instead of the arrow a semi transparent pill with a downward arrow in it". On the home page only (`body.home-page`, `src/nav-reveal.ts`): the bar is parked above the top of the screen and all that shows is a semi-transparent pill with a downward chevron, which nudges itself every few seconds. Pointer into the top 88 px and the bar drops in, the pill rides down under it and the arrow turns over; five seconds after the last interaction up there it parks again. It holds while the pointer is on it or something in it has focus, a tap toggles it (a finger has no hover), Escape closes it, and it never opens while the package is still wrapped |
| He The Truth, peel to the bottom left | Lawrence: "add this as another track to the site. it should be right after let him cook" and "the peel animation should fall off to the bottom left in the direction of the peel". `He The Truth` (3:03) is track 6 and Faith moved to 7: `scripts/lit-tracks.json`, a new 30-second preview (`npm run previews`, `06-he-the-truth.mp3`, Faith renamed `07-faith.mp3`), the full song and the rebuilt album zip uploaded to both Convex deployments (`npm run upload:audio` and `--prod`, everything else unchanged), and the sleeve's printed back now lists seven tracks. The plastic follows the peel instead of going back the way it came: the fold has always swept from the top-right corner towards the bottom-left, so the coil is now dragged that way as it rolls (`uDrag` along `-uDir`) and the whole wrapper is thrown off to the bottom left, down and out of frame, rather than to the right |
| Backdrop | Lawrence: "the city backdrop needs to be a real 90s anime render of a cyberpunk city". Replaced the procedural city/haze/grid with a painted backdrop: Midjourney Niji 6, round 2 option 3 (`public/assets/images/minidisc/city-backdrop.webp`). All 8 generations are kept in `~/Downloads/CD PLYAER MYINDSOUND/backdrops/`. The procedural city stays as the fallback if the image fails to load |
| Production domain | `stream.myindsound.com` (Netlify site `myindreleases`, builds branch **`master`**). Answers open question 2 |
| Physical store | Runs on Shopify Storefront (`VITE_SHOPIFY_*`). `create-physical-checkout` was dead code and is deleted. Answers open question 6 |
| Clerk | The live site uses the Clerk **dev** instance `main-grouper-12` (`pk_test_…`). Dev instances cap users and show dev banners; moving to a production instance is a follow-up |
| Netlify env today | Only the Stripe live keys, Clerk publishable key, Supabase and Shopify vars. No webhook secret, Clerk secret or GHL keys, so the old webhook could never have provisioned access |
| Convex | Team `myindmedia`, project `myind-releases`, dev deployment `decisive-iguana-954` (products and 6 tracks seeded) |
| Admin | Enforced server-side: `users.isAdmin` or `ADMIN_EMAILS` (replaces the client-side hardcoded email check). `admin.html` was missing from the Vite build and is now included |
| Key feel | Latched keys sink 60% and darken 25%; a momentary press goes full depth for 140 ms |

### Still open
1. Album title: "Live In Truth" vs "Living In Truth" (track 1 title currently "L.I.T. (Living In Truth)").
2. Cloudflare account with R2 enabled, plus an R2 API token.
3. Clerk: done on the Myind Sound Releases instance (dev). A production Clerk instance (custom domain + DNS) is still open.
4. A Stripe **test** key for the Convex dev deployment (Netlify only has live keys).
5. GHL API key and location ID (never set on Netlify).
6. PostHog: cookieless mode or a consent banner.
7. `stream/` legacy React app: delete it or keep it?
8. `/pentest` needs Docker (not installed here) or a Strix Cloud account.
| Disc art | Lawrence: "this should be the disk image" (clean LIT cover, samurai in the neon street). Saved as `~/Downloads/CD PLYAER MYINDSOUND/lit-cover-clean.png`; `build_disc_face()` in `scripts/build-player-assets.py` prints it on the disc (84% inset, blurred bleed, real Canva hub, grooves, rim). The clear disc overlay dropped to 22% so the art reads |
| Cartridge label | Lawrence: "for the disk label it should be the do not duplicate label". It's cut from Canva page 14 (`DND_LABEL` in `scripts/build-player-assets.py`) and replaces the Myind Sound label (page 2). It sits 0.045 world units lower than in the Canva comp so the window frame doesn't clip the handwriting |
| City backdrop v2 | Lawrence: "pixel perfect match the uploaded image … comic book 90s anime texture … slightly 3d", plus earlier "glowing pulsating neon lights in the distance, flying vehicles in the distant sky". Built:<br>• Nano Banana Pro restyle of `Covers Albums.png`, fed padded to 16:9 and cropped back so edges line up (edge correlation 0.68 vs 0.27 unpadded)<br>• Outpainted to 21:9 with the original centre composited back in<br>• Gemini depth map giving slight parallax<br>• Neon and beam mask, flying craft only in the far sky, embers and rain<br>The procedural 3D city and the Niji painting were both rejected |
| Halo | Lawrence: "the hud circular halo should be over the disk area and only appears before the disk starts to play … like a halo". It's a 3D ring (`halo.ts`) at z +0.34 in front of the disc. It boots on insert and on calibration, and its ticks fill clockwise (with RPM on insert, over time on calibration). It dissipates on playback. The HTML disc ring is gone |
| Track selection | Lawrence: "load delay in song selection for at least 2 seconds with a loading laser calibration sound". Selecting a track while running (tracklist click, ◀ ▶ while playing) enters `seeking`: music paused, synthesised calibration sound, halo, disc dip to 35%, then play after ≥ 2.2 s. ◀ ▶ while paused or stopped just move the cursor. Auto-advance at the end of a track stays gapless |
| React Bits Pro | Lawrence: "use some of the reactbits pro assets for the hud and 3d elements and effects". Pro is 599 app-UI blocks, not the WebGL effects library. Ported to vanilla TS in `hud-fx.ts`:<br>• `404-6` glitch → status text<br>• `404-8` + `cta-12` typing → boot terminal<br>• `bento-41` → oscilloscope<br>• `monitoring-2` → signal sparkline<br>• `analytics-10` → RPM gauge<br>• `404-7` radar → halo sweep<br>Originals are in `src/vendor/reactbits/`. The licence key was read from `~/Dev/MTTE/.env.local`, because the 1Password service account can't see the Employee vault |
| Eject inspector | Lawrence: "an eject button on the red player button that should eject the minidisk and show it in 3d … in the foreground … 360 rotation … zoom into details … from all angles. The insert disk tab pushes it back in", then "the side profile … should have the same texture all the way around". Decisions:<br>• Red key = Eject, with an eject glyph printed on it. Repeat moved to a REPEAT toggle under the tracklist; R still toggles it and E ejects<br>• Eject works from playing, paused, stopped or calibrating. Audio fades, the disc spins down, the spring pops the cartridge out of the slot, and it flies forward with one turn (`ejecting` → `ejected`)<br>• The inspector rotates the cartridge, not the camera, so the backdrop and HUD stay put. Deck and city dim behind it, and the CRT pass softens<br>• Push back in (INSERT DISC, Play, Enter/Esc, or a track pick) flies it home from wherever it is and plays that track from the start<br>• All sides are real: back cap from Canva page 6 (metal hub), solid rim-textured edges, shell texture raised to 2048 px so zoom stays sharp |
| Opens ejected | Lawrence: "the site should start with the disk floating in ejected mode always". The deck boots straight to `ejected` (the `empty` state is gone): the cartridge drifts in and floats in front of the empty deck, with INSERT DISC and the inspect hint below. Insert, Play, Space or a track pick loads it |
| Stream page live (demo) | Lawrence: "publish to https://stream.myindsound.com/stream" (2026-09-16). Published as manual Netlify deploys (latest `6aac1fe36514df3df4853d9b`, 2026-09-17, open stream page on production Convex: buyers full songs, others previews): the live site's files and functions from commit `1e5c3df`, byte-for-byte, with only `/stream` swapped for the 3D player in demo mode (`VITE_PLAYER_DEMO=1`, demo clip, no sign-in). Rollback: restore deploy `698eeb90a077b40008274af6`. The next push to `master` replaces it. Local `master` (e61ad0f) has diverged from `origin/master` (1e5c3df); reconcile before merging the branch |
| Cartridge realism | Lawrence: "make sure the elements of the mini disk are 3d like the screws and the textures so its more realistic, leveraging threejs". `cartridge-detail.ts`:<br>• 8 steel Phillips screws placed where the build script detects them on the art<br>• Steel clamp ring on the disc and hub ring plus chrome cap on the back<br>• Bevelled shell edges; clear-plastic clearcoat with normal maps from the artwork's grip texture<br>• Rainbow sheen on the disc; label as raised paper with fibre grain and visible thickness<br>• Neon-tinted studio reflections; a camera-fixed key light while floating; the pink and ice accent lights dim to 25% so edges don't blow out<br>The Canva art itself is untouched; the layers only add depth and light. 121 fps on desktop; +20 KB gzip JS, +220 KB textures |
| No see-through | Lawrence: "ensure areas that are transparent still that aren't supposed to be are addressed". Audit: every texture is solid inside its outline (only the deck window hole and glass glare are meant to be clear). A magenta-background check found two 3D leaks, both fixed: the cartridge was hollow (back cap and sides only rendered from outside, so you saw past the disc edge through the window), and the front shell discarded pixels where the art's outline is smaller than the 3D shape. The shell is now art over dark plastic, fully opaque outside the disc window; back cap and sides are double-sided. Seated deck, tilted deck and empty deck window checked clean |
| Screw wells | Lawrence: "ensure you compensate for the indentations and openings where the screws are". Each screw now sits in a counterbore: the opening is cut out of the cap (shader discard on the front, alphaMap on the back and on the gloss coat), a dark plastic well (wall + floor, 1.38× the head radius, 0.008 deep) sinks below the surface, and the head rests on the floor, below the surface. Front and back |
| Eject feel + thickness | Lawrence: "increase the disk slow down on ejection to about 3 seconds" and "make the mini disk a little bit thicker". Eject now winds the disc from 300 RPM to 0 over 3 s (`EJECT_SPIN_DOWN_SECONDS`) before the door opens and the cartridge pops (about 4.7 s in all). `CART_DEPTH` 0.036 → 0.048 (5.7% of the width; a real MiniDisc is about 7%); the slot and door follow it |
| Disc depth + separate hub | Lawrence: "make the disk and the center hub on the disk seem like two separate things … not one layer" and "I need the disk to have depth along with the center hub". The disc is now a 0.008-thick platter (art top face, outer edge, inner wall, iridescent silver data side). The printed hub is gone from the art: the disc has a centre opening with a soft contact shadow, and a separate machined steel hub (bevelled plate, raised steel ring, spindle hole, two dimples) sits in it, standing about 0.003 proud and spinning with the disc. Sizes in `geometry.json` `disc.hub` |
| Back opening + spindle | Lawrence: "the area on the bottom of the disk should be an opening at the center hub; there should be a spindle in the player where that portion of the spinning disk will touch the spindle to make it spin". The back of the shell has a real opening at the hub (centred on the disc; the steel bezel is sized to cover the back art's off-centre printed ring), with a plastic wall up to the disc, and the hub now has a real centre hole. `spindle.ts`: a motor on the tray (stator, column, chuck with three magnet pads, chrome shaft). It sits dropped while the cartridge slides in, rises once seated (chuck pads meet the hub plate, shaft enters the hole), turns with the disc, and drops clear after the 3 s spin-down before the eject |
| Song changes (bug) | Lawrence: "the song isn't changing when I select a different song or click the forward or back buttons". Root cause: the public demo pointed all 6 tracks at one file (`Cook-Demo.mp3`), and `AudioEngine.load()` skipped loading when the file matched, so the same song carried on. Fixed both: `load()` now always restarts (tested), and the demo plays a 30-second preview of each real LIT song (Lawrence chose previews over the full songs; `npm run previews`, `public/assets/audio/lit-previews/`, starts 25% in unless `previewStart` is set in `scripts/lit-tracks.json`). The full songs stay paid |
| Disc mechanics sound | Lawrence: "use this sound file for the disk mechanics … 0:00-0:03 loading and spin up, 0:04-0:10 spinning (loop while spinning), 0:11-0:13 stop … make sure the sound markers match the animation. The sound starts after the disk is loaded". `npm run disc-sounds` cuts `~/Downloads/CD_Drive_06_11.wav` at the measured boundaries: spin-up 0-3.8 s (clamp clunk at 0.85 s, motor from 2.55 s), a 6 s crossfaded loop from 3.8 s, spin-down 10.6-13.39 s (brake click at 1.0 s). The deck follows RPM curves read off the same envelopes (`disc-sounds.json`). Insert: sound at seat (1.8 s), spindle clamps on the clunk, playback at 5.6 s as the loop takes over. Pause, stop, end of album and eject play the spin-down (the eject wait is now its 2.79 s). Resume skips the loading clunks. The loop follows disc speed during calibration |
| Spin-up before play + unload sound | Lawrence: "for the play pause function the disk needs to spin up before audio can play. also play the cd load in reverse before the eject animation. so we have disk load and unload audio". Play after a pause or stop now enters `resuming` (HUD: SPIN UP): the disc follows the spin-up curve (joined at its current speed, so a disc still winding down picks up from there) and the music starts only at full speed, about 2.1 s from still. Pause, stop, eject, ◀ ▶ still work while it spins up; pausing before the disc moves cancels the spin-up sound. Eject: after the spin-down, `disc-unload.mp3` plays the load sound (0-1.45 s of the recording). First built reversed; Lawrence then asked "keep the eject sound just change it to not be reversed audio", so it plays forward: the spindle drops on the first clunk (0.16 s) and the cartridge pops on the big clunk (0.96 s). About 5.2 s from playing, 3.0 s from paused. Checked headless against logged sound starts and RPM |
| Flat HUD panels | Lawrence: "remove the tilt effects from the track list and Deck interface. those should just be static and flat". The HUD layer (tracklist, DECK · MD-01 readouts, spectrum) no longer rotates with the pointer: the perspective transform on `.p3d-hud` and `Hud.setTilt` are gone. The 3D deck still tilts, and the city still parallaxes with it |
| LCD replaces the halo | Lawrence: "remove the calibration halo animation and the sound. but on the player lower left corner of the black cover put calculator style led screen that will show player status, Calibrating, loading, ejecting, and button mode status". Removed `halo.ts` and the synthesised laser sound (`calibration-sound.ts`); selecting a track still waits 2 s (CALIBRATING). New display (`lcd.ts`, `lcd-text.ts`) in a recess in the cover's lower-left corner, mirroring the Myind Sound badge: amber 14-segment characters with faint unlit segments, play/pause/stop/repeat flags, deck status with the track number, and a 0.9 s flash for ◀ ▶ and repeat. Amber matches the HUD gold. The HUD status pill above the deck is still there |
| Picture leads the sound | Lawrence: "the disk should spin first then trigger the spinning sound so its more realistic", and "remove the floating stop label on the top of the player". The deck's spin-up curve now starts where the recording's motor takes hold (`SPIN_UP_MOTOR_AT` 2.55 s) and the sound plays from `SPIN_LEAD_SECONDS` (0.2 s) earlier, so the disc is already turning when the whine arrives. A resume no longer waits through the loading clunks: the disc moves the moment you press play and the music starts 1.25 s later (was 2.1 s); the insert's playback starts at 5.4 s. `resumeFrom` is gone from the manifest. The floating status pill above the deck is removed (the LCD and the phone strip carry the status; a 1x1 live region still announces it), and a media READ ERROR now flashes on the LCD |
| Launch checks | Goal: "get everything up and running to be live now so I can start to share it". Stripe live webhook created against `https://loyal-tortoise-999.convex.site/stripe/webhook` (`we_1UGoVbGdTSTdmkS7raNb7wVx`, checkout.session.completed + async_payment_succeeded) and its signing secret stored on prod; the endpoint answers 400 to unsigned posts. Past buyers imported: `payments:rebuildFromStripe` granted 3 of 3 sessions. That import first failed 3/3 with a swallowed error: `CLERK_SECRET_KEY` had been piped in with a trailing newline, so every Clerk call returned 401. The catch now logs the reason (counts only, no customer data), and both deployments hold the trimmed key. Checked live: all six pages 200, the phone layout has no sideways overflow, the physical store lists 17 products, a live checkout session creates, previews and packaging assets serve, and Lawrence's own account gets the six full songs from prod |
| Live on production | Lawrence: "ok push to production" (and confirmed the Clerk cutover). `main-grouper-12` turned out not to be in his Clerk account at all, which is why nobody could add the JWT template; his account has a **Myind Sound Releases** app, so the CLI created the `convex` template there (`clerk api jwt_templates -X POST`). Dev and prod Convex now carry that issuer and secret, Netlify has the matching publishable key plus `VITE_CONVEX_URL=https://loyal-tortoise-999.convex.cloud`, the functions were deployed to prod (the `convex deploy` prompt needs a pty: driven with `expect`), and `feat/convex-3d-player` was merged with `origin/master` and pushed. The first build failed because `prebuild` runs `convex codegen`, which cannot authenticate on Netlify; it is non-fatal now, since `convex/_generated` is committed. Deploy `6aac70669b62390007931bb7` is live: the home page opens on the wrapped disc, `/stream` 301s to `/`, and the login page talks to the new Clerk instance. **Still open:** `STRIPE_WEBHOOK_SECRET` is not set on prod, so a live purchase does not yet grant a permanent entitlement (the 24-hour unlock does work), and the Clerk instance is still a development one (a production instance needs DNS) |
| Disc sits higher | Lawrence: "the spinning disk in the minidisk needs to sit higher up closer to the plastic cover layer". `DISC_TOP_Z` went from -0.003 to 0.011, so the disc's art is 0.013 under the shell's front face instead of 0.027. The hub, the back opening wall and the spindle travel all key off that constant, so they followed; checked seated and playing (PLAY 01 at 300 RPM) and floating, front on and tipped |
| Site styles back on the home page | Lawrence (screenshot): the COMING SOON cards, the physical CTA and the pay-what-you-want modal were unstyled. The rewritten `index.html` had dropped `<link rel="stylesheet" href="/style.css">`, the site stylesheet at the repo root that carries the cards, buttons and modal; only the player's own CSS was loading. It is back, and because that file also styles `.stream-page .main-nav .nav-links a`, which outranked the player's pill rules, the nav selectors in `hud.css` were raised to match (and the site's solid bar and active underline are cleared over the player). Also: starting the unwrap now resets zoom and pan to the default framing, not just the rotation |
| Same position throughout | Lawrence: "position of the disk should be the same position always, it should be in the final position the main disk after fade in is". The opening scene had its own framing (centred, two thirds of the viewport height), so the package jumped when the sleeve came off. The inspector now measures the same `.p3d-stage` box whether the cartridge is wrapped or bare, and the unwrap hint sits where INSERT DISC appears, so the package, the disc and the button all stay put |
| No flash of the disc | Lawrence: "im also seeing a flash of the minidisk on the first load, it shouldnt be revealed until the unbox animation". The float-in made the cartridge visible as soon as the tracks loaded, and the packaging was only built after the sleeve art and film textures had downloaded, so the bare disc showed in between. The cartridge is now hidden as the opening scene starts and only shown again once `DiscWrap` has been constructed around it (and on the texture-failure path, where the page just opens as itself). Checked across the first eight frames of a cold load: the wrapped package and nothing else |
| Black until it's open | Lawrence: "when the site first loads it should only be the box and the double click instructions and a black screen, nothing else fades in until after the unbox animation is finished". The nav was still on screen (dimmed) and the page could be scrolled to the sections below, and because the intro class only landed after mount, the HUD flashed and then faded out over its own transition. Now `home.ts` marks the page sealed before the player mounts, the intro state has no transition going in (only coming out), the nav is hidden with it, the boot overlay is skipped in wrapped mode, and html and body are locked at `overflow: hidden` until the reveal. Checked from 0.9 s to 6 s: nothing but the package and the hint |
| Sleek peel, slow fade | Lawrence: "the plastic peel needs to be different, this doesn't look sleek. also the animation after the peel needs to disappear after too. The package should recenter to the front view before animation", then "the fade in after animations needs to be slow". The coil is tighter (radius 0.027, drag 0.14 of the width) with the wobble and wander gone, and the film is glossier (roughness 0.1, clearcoat 0.9), so it reads as one clean sweep rather than a flapping sheet. The wrapper now shrinks as it exits, goes 3.6 cartridge-heights out and is hidden outright at the end (checked: hidden by 4.5 s, disposed by 7.5 s). Double-click settles the package square to the camera first and the sequence starts 0.52 s later. The reveal fade is about three times slower now (blackout easing 0.6, HUD transition 2.6 s): measured black 0.99 to 0.35 to 0.09 to 0.01 |
| Peel re-cut | Lawrence: "why is there a two tone line around the entire bottom portion. also fix the peel animation and sound, it should be just once. also it is peeling off weird". Three fixes. The band was the printed panels being inset by the card thickness, so a strip of bare sleeve showed round the art, doubled by a stretched slice of the cover on the bottom face: the print now runs to the edge and the foot is plain card. The peel looked like a flat sheet sliding across the face because the curl radius was 0.08 with the coil dragged half the package width sideways: the radius is 0.034 and the drag is 0.16 of the width, mostly towards the viewer, so it coils and lifts. And the sequence is quicker (peel 1.85 s, 5.4 s in all) with exactly one peel sound (slowed to the phase) and one hand on the sleeve, verified in the browser as two scheduled sounds |
| Film on every face | Lawrence: "the plastic wrap needs to be on all layers, its only on the front layer not the sides or the back". The back and the four sides are wrapped again, each with its own slice of his sheet so the crinkle carries round the package, all front-side only and parented to the peeling group, so it is still one layer per face and the whole wrapper comes off together. (The screen recording he linked had already been cleaned up by macOS, so this went on his description.) |
| Opaque sleeve | Lawrence: "the sleeve seems to be semi transparent. please fix it". The film had `depthTest: false` (added so the peeled plastic could not be clipped by the package), which meant the front sheet also drew through the sleeve when it was turned round, bleeding the cover over the back. Depth testing is back on; the plastic still hangs clear of the package on its own, because the curl is dragged off it by `uDrag` |
| Real unwrap sounds, 360 sealed view | Lawrence supplied `Medical Unwrap Item Plastic Wrap Peel Adhesive 01.wav` for the peel and `HumanHand 6106_20_1.wav` for the sleeve pull, and asked for the sealed product to be turnable with a printed back. `npm run wrap-sounds` normalises and trims both into `public/assets/audio/wrap/` (0.69 s and 0.16 s one-shots); `wrap-sound.ts` layers the peel across its phase at falling speed and volume (15 passes) and lands the hand on the sleeve coming off (18 hits in all, verified in the browser). The sealed package now turns: drag, arrows, pinch and wheel work while it's wrapped, while a double-tap unwraps instead of resetting, and the hint reads DRAG TO TURN. The back of the sleeve is printed for real: LIT, THA MYIND / MYIND SOUND, the six tracks with times, the imprint lines and a barcode, aged over the top |
| Plastic peels over the screen | Lawrence: "the plastic peel animation stays contained in the container of the sleeve. so when it peals it gets cut off. it needs to peal off into the screen as an overlay". The curl used to roll up inside the sheet's own rectangle, so it never left the package outline. A `uDrag` uniform now pulls the rolled part along the peel direction and towards the camera as the fold travels (half the package width by the end), and the film draws with `depthTest: false` at renderOrder 60, above the inspector's dim plane, so the loose plastic hangs off the package and over the screen instead of being clipped by it |
| Sleeve wraps the disc | Lawrence: "the sleeve should be a threejs layer sleeve around the mini disk". The board is thicker (0.006 vs 0.0025) with a wider gap round the cartridge, the cover print now carries over the spine and the foot (thin strips sampling the edge of the cover, the same trick the cartridge rim uses), and the cartridge stands 7% of its height proud of the sleeve's mouth under the same film, so the package reads as a disc in a sleeve rather than a flat plate |
| Supplied packaging art | Lawrence: "update the sleeve to this and plastic cover [images]". The sleeve cover is now the clean LIT art (`public/assets/images/lit-sleeve.webp`, from `~/Downloads/¡ (1).png`, 98 KB), replacing the 30 MB print poster and its LIMITED OFFER sticker, and the film is rebuilt from his newer sheet (`~/Downloads/Untitled Design (1).png`) as alpha plus a derived normal map. Soft diagonal folds now, no hard frame around the edge |
| One plastic, two beats | Lawrence: "im still seeing 3 steps. the first plastic, a second one from the back, and the sleeve. it should just be one plastic then the sleeve", with his own sleeve and plastic layers attached. The back and side film pieces are gone: there is now a single sheet, textured from his shrink-wrap PNG (crinkle in the alpha, relief in a normal map, built into `public/assets/images/minidisc/wrap-film*.webp`). The timeline is two beats, plastic off (peel 0.5-3.6 s, carried out of shot by 4.6 s) then sleeve down and off screen (4.7-6.5 s), and the rolled lip now brightens as it curls so you can see where the plastic went. His sleeve layer (grey with the Myind Sound band and the red tear tab) is not used yet: the sleeve front is still the LIT cover |
| Critics' pass | Lawrence: "why is there something floating on top? the plastic ... please have your critics look at it all". The floating object was the sleeve's extruded card tube, translated a full height up instead of centred. Two reviews then ran, one on the code and one on the visuals. Fixed from them: the 30 MB print poster gated the opening (a 1600 px WebP now, and the deck, city and HUD go dark before it loads, so no flash of the finished site); the wrapped package was still interactive, so Enter or Space inserted the disc into a hidden deck; the canvas swallowed page scroll (wheel now only zooms over the cartridge, `touch-action: pan-y`); the HUD stayed in the tab order behind the black (`inert`), and the hint is a real button; the checkout session id reached PostHog in the pageview URL (stripped before analytics starts); grains longer than the noise buffer played silence; the wrap is disposed and its listeners aborted; bloom and the key light are turned down for the intro, which took the blown-out area of the package from 28% to under 5%. Still open: coarse-pointer quality tier for the wrap, Convex storage links never expiring, rate limiting `streamForCheckoutSession`, renderer sizing against `100svh` on iOS, and the critics' pacing note (7 s was Lawrence's call) |
| One film, real sleeve | Lawrence: "there are two layers of plastic. it should only be one. also the sleeve needs to be a full 3d asset surrounding the disk. it should slide off the screen not fade out then fade in the rest of the site". The film is a single layer now (front-side only on the closed pieces, so the far side is never seen through the near one, and it hugs the sleeve at 1.5 mm). The sleeve is a real extruded card sleeve: walls right round the cartridge with thickness, a closed foot and an open mouth the disc slides out of, with the cover and the worn back printed on it. The reveal moved to 7.15 s, after the sleeve has left the screen, so the site fades in behind a bare disc rather than over a moving sleeve |
| Peel, then slide off | Lawrence: "the unwrap animation should just be plastic unwrap animated peel and the sleeve slides down off the page not dissolve". The film is now one piece: the front sheet peels and drags the back and sides with it (they are parented to the peeling group), and the whole wrapper travels out of shot instead of crumpling and fading. The sleeve slides straight down past the bottom of the screen at full opacity (y 0 to -1.97, about 3x the cartridge height) and the disc is left bare. No opacity fades anywhere in the sequence |
| Slower unwrap | Lawrence: "need the plastic to peal off and the unwrap animation slowly and use threejs to do it". The sequence runs about 7.3 s now instead of 3.5 (`wrap-math.ts`: squeeze 0.7, peel 3.1, film off 1.8, sleeve 1.7), the curl radius is bigger so the roll reads, the film is a touch less clear so you can see the sheet lifting, and the remaining pieces are pulled off their own faces (each along its outward normal, crumpling and turning) instead of just shrinking. The synthesised sound follows the same constants, so it stretches with it |
| Unwrap polish | Lawrence: "the disk is getting unsleeved facing backwards, it needs to be facing forward when peeled and de-sleeved. make the animation nicer and more dynamic", and "make the back part of the plastic wrapper look a bit worn and weathered as its supposed to be a relic in a dystopian future". Root cause of the backwards disc: the inspector's idle showcase turn (0.35 rad/s) kept turning the package, so where it ended up was luck. It now sways around square-on instead (wrapped or bare), and the sleeve phase eases it back to front, measured at 0.97-0.99 facing the camera from sealed to 11 s after the reveal. The fold now wanders and breathes as it travels, the film crumples piece by piece and the rolled sheet arcs out of shot spinning, and the sleeve gets a tug before it is pulled away. The back of the sleeve carries a faded back-cover print, stains, creases, scuffs and knocked corners under cloudy scratched film |
| Real 3D packaging | Lawrence: "I need the actual 3d mini disk to be plastic wrapped with the poster sticker layered over it as the packaging … what you have is just a sticker floating in mid air … double clicking the disk will peel off the wrapping animation, with sound, and then reveal the disk. This should all be in a black background. once the disk is revealed then the main screen fades in". Replaced the DOM sticker with `wrap.ts`: clear film (physical material, crinkle normal map, iridescence) over the cartridge with the poster laminated on the face, parented to the cartridge so it turns with it. The unwrap is a fold running across the face in the vertex shader (the poster and the front sheet curl off together), then the rest of the film shrivels and the rolled sheet flies out of shot, 2.6 s, with a synthesised crinkle-and-tear (`wrap-sound.ts`) on the same gesture that unlocks audio. The page opens with the inspector in intro pose: package centred on black, deck, city and HUD hidden, all fading in as the black goes. Two traps found: the city's sky plane rides on the camera, so hiding the city group left it up (`ComicCity.setVisible`), and a coming-soon card's peel flap hangs a card-height above it, which put a ghost sheet on the black screen (clipped) Then: "the wrapping and poster image needs to be edge to edge and should be a sleeve that the disk slides out of once the plastic is removed" - the poster is now a printed sleeve around the cartridge (cover-fitted so it fills the face without stretching) inside the film, and once the plastic is off the sleeve slides down and leaves the disc (3.5 s in all) |
| Lean towards the cursor | Lawrence: "invert the mouse hover animation for the player so things lean toward the cursor vs away". The deck's pointer tilt is negated in `scene.ts` (`updateTilt`), so the edge nearest the cursor comes forward. The city parallax reads the deck's rotation, so it follows the new direction too. Measured: cursor top-right gives yaw -22 deg, pitch +12; bottom-left gives +21 and -7 |
| Home = the player, wrapped | Lawrence: "we need to make the main page the stream page. lets consolidate. I want people to see the lit poster from the main page that will be the wrapper for the mini disk. They can click to peel off the wrapper revealing the disk inside. then they can load it. They can only sample the songs unless they pay". Decisions taken with him: the player fills the first screen (presale and physical scroll below), the poster peels in 2D in front of the deck (the React Bits sticker port, not a 3D sleeve), and paying unlocks the full album for 24 hours from the Stripe session without signing in. Built: `index.html` is now the player page (`src/home.ts`), `/stream` and `/stream.html` 301 to `/`, the superseded home files are gone (`main.ts`, `stream.ts`, `purchase-animation.ts`, `disk-player.ts`, `lit-hover.ts`, `stream.html`), GET LIT opens the PWYW modal on the page, and `payments.streamForCheckoutSession` + `purchase-session.ts` carry the 24-hour unlock. The cartridge floats in behind the poster so peeling reveals it in place; the inspector's home pose was flipped, because it had been showing the cartridge's back |
| Songs in Convex storage | Lawrence: "these should be the full songs for folks who have purchased the album" and "the songs should be loaded to a storage on convex" (replaces the Cloudflare R2 decision). The full songs, track 3's WAV original and the album zip live in Convex file storage (`npm run upload:audio [--prod]`, reruns skip unchanged files). `@convex-dev/r2` is removed. Trade-off accepted: storage links don't expire, so only purchase-checked functions return them. The stream page is open to everyone: buyers get the full songs, everyone else the previews with a SIGN IN / GET LIT note. Production Convex `loyal-tortoise-999` was created and deployed 2026-09-17, with `CLERK_JWT_ISSUER_DOMAIN`, `SITE_URL` and the live Stripe key (copied from Netlify, never shown) set, and the album uploaded. **Still blocked:** buyers can't sign in to Convex until the Clerk `convex` template exists, and importing past buyers (`payments:rebuildFromStripe`) needs `CLERK_SECRET_KEY` |
